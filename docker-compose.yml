version: "3.9"

services:
  web:
    build: .
    container_name: skew_batch
    ports:
      - "5000:5000"
      - "8050:8050"
    environment:
      FLASK_ENV: production
      PYTHONUNBUFFERED: "1"
      TZ: Asia/Kolkata
    volumes:
      - ./PLCDB2.db:/app/PLCDB2.db
    restart: always